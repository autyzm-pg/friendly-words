import React from "react";
import CapriolaText from "./CapriolaText";
import glamorous from "glamorous-native";

export const Header = glamorous(CapriolaText)({fontSize: 50 });
export const JumboHeader = glamorous(CapriolaText)({fontSize: 60});